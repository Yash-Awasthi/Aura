<?xml version="1.0" encoding="utf-8"?>
<!-- Task 71 — Circular progress arc for the 2-second enrollment capture window.
     Included inside fragment_gesture_enrollment.xml. The [progress] attribute
     is driven by EnrollmentCaptureState.Capturing.progressFraction * 100. -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/arcProgress"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        app:trackThickness="8dp"
        app:indicatorColor="?attr/colorPrimary"
        app:trackColor="?attr/colorSurfaceVariant"
        android:progress="0"
        android:max="100" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tvProgressPercent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textAppearance="?attr/textAppearanceTitleMedium"
        android:text="0%" />

</FrameLayout>
